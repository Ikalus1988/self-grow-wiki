# RAG Knowledge Base — Python dependencies
# Install: pip install -r requirements.txt

# Core RAG
chromadb>=0.5.0
openai>=1.0.0

# Mixed retrieval (BM25 + vector)
rank-bm25>=0.2
jieba>=0.42

# Optional: cross-encoder rerank (improves top-k sorting)
# sentence-transformers>=2.2
# torch>=2.0

# Optional: WeChat bot (Windows only, via wxauto)
# wxauto>=0.9.0
# Pillow>=9.0

# Server (for HTTP API)
# fastapi>=0.100
# uvicorn>=0.20
